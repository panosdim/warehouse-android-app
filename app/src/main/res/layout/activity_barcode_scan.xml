<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="com.padi.warehouse.activities.BarcodeScanActivity">

    <!--  set custom layout for scanner-->
    <com.journeyapps.barcodescanner.DecoratedBarcodeView
        android:id="@+id/zxingBarcodeScanner"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:zxing_scanner_layout="@layout/custom_scanner" />

    <!--  FlashLight Button-->
    <ToggleButton
        android:id="@+id/switchFlashlight"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:showText="true"
        android:splitTrack="true"
        android:switchPadding="@dimen/appbar_padding_top"
        android:textAlignment="viewStart"
        android:textColor="@android:color/white"
        android:textOff="@string/flashlight"
        android:textOn="@string/flashlight"
        android:textStyle="bold" />
</RelativeLayout>
